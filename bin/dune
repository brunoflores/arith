(ocamllex
  (modules lexer))

(menhir
 (modules parser)
 (flags --table --strict))

(executable
 (public_name arith)
 (name main)
 (modules main support syntax lexer parser arith)
 (libraries arith core cmdliner menhirLib)
 (preprocess
  (pps ppx_inline_test ppx_jane ppx_deriving.show)))
